# LMU Datathon — EMS Visualizations

Visualizations from the LMU Datathon EMS call analysis project.

## Contents

- **heatmaps/** — Call volume by hour of day
  - `heatmap_combined_all_bases.png` — System-wide view
  - `by_base/` — Per-base heatmaps
- **clock_charts/** — Response time clock charts
  - `ems_response_time_clock.png` — Combined view
  - `by_base/` — Per-base clock charts
